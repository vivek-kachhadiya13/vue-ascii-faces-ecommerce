// base API URL
export const API_URL = 'http://localhost:3001';

// fetch limit on each request
export const FETCH_LIMIT = 20;

// scroll threshold to fetch new entities
export const FETCH_SCROLL_THRESHOLD = 200;

// base ADS URL
export const ADS_URL = '/ads/?r=';

/**
 * availables entities on the app that can be fetched from the API
 * if need just add a new one and the app will handle everything automatically
 */
export const PRODUCTS_ENTITY = 'products';
